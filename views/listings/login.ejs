<link rel="stylesheet" type="text/css" href="../css/slider.css">
<div class="flash-container">
  <% if ((success && success.length> 0) || (error && error.length > 0)) { %>
    <div class="flash-message <%= success && success.length ? 'flash-success' : 'flash-error' %>">
      <%= success && success.length ? success[0] : error[0] %>
    </div>
    <% } %>
</div>
<div style="min-height: 100vh; background: linear-gradient(135deg, #1d2b64, #f8cdda); display: flex; justify-content: center; align-items: center; font-family: 'Poppins', sans-serif;caret-color: transparent;">
    <div style="background: rgba(255, 255, 255, 0.1); border-radius: 16px; padding: 40px 30px; width: 100%; max-width: 420px; backdrop-filter: blur(15px); box-shadow: 0 8px 32px rgba(0,0,0,0.2); border: 1px solid rgba(255,255,255,0.2);">
  
      <h2 style="text-align: center; margin-bottom: 30px; color: white; font-weight: 600;">🚀 Log In for Eco-Vision</h2>
  
      <form action="/login" method="POST">
  
        <label for="username" style=" caret-color:transparent ;  color: #fff; font-weight: 500;">Username</label>
        <input type="text" name="username" id="username" required style=" width: 100%; padding: 12px 15px; margin: 8px 0 18px; border: none; border-radius: 8px; background: rgba(255,255,255,0.2); color: white; outline: none; caret-color: white; ">

        <label for="password" style="caret-color:transparent ; color: #fff; font-weight: 500;">Password</label>
        <input type="password" name="password" id="password" required style="width: 100%; padding: 12px 15px; margin: 8px 0 25px; border: none; border-radius: 8px; background: rgba(255,255,255,0.2); color: white; outline: none; caret-color: white;">
  
        <button type="submit" style="width: 100%; padding: 12px; background: linear-gradient(135deg, #6dd5ed, #2193b0); color: white; border: none; border-radius: 30px; font-weight: bold; cursor: pointer; transition: 0.3s;">
          Log In
        </button>
  
      </form>
    </div>
  </div>